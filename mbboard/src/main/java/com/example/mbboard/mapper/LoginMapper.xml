<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mbboard.mapper.LoginMapper">
	<!--로그인-->
	<select id="login" resultType="com.example.mbboard.dto.Member" parameterType="com.example.mbboard.dto.Member">
		select member_id memberId, member_role memberRole from member
		where member_id = #{memberId} and member_pw = #{memberPw}
	</select>
	
	<!--멤버 목록-->
	<select id="memberList" resultType="com.example.mbboard.dto.Member" parameterType="com.example.mbboard.dto.Member">
		select member_id memberId, member_role memberRole from member
	</select>
	
	<!--아이디 중복검사-->
	<select id="selectId" resultType="string" parameterType="string">
		select member_id memberId from member where member_id = #{memberId}
	</select>
	
	<!-- 아이디에 맞는 비밀번호-->
	<select id="selectPw" resultType="string" parameterType="string">
		select member_pw memberPw from member where member_id = #{memberId}
	</select>
	
	<!--회원가입-->
	<insert id="insertId" parameterType="com.example.mbboard.dto.Member">
		insert into member(member_id, member_pw, member_role) values(#{memberId}, #{memberPw}, #{memberRole})
	</insert>
	
	<!--역할수정-->
	<update id="updateRole" parameterType="com.example.mbboard.dto.Member">
		update member set 
		<if test='memberRole=="ADMIN"'>
			member_role = 'MEMBER' 
		</if>
		<if test='memberRole=="MEMBER"'>
			member_role = 'ADMIN' 
		</if>
		where member_id = #{memberId}
	</update>
	
	<!--비밀번호 수정-->
	<update id="updatePw" parameterType="com.example.mbboard.dto.Member">
		update member set member_pw = #{memberPw} where member_id = #{memberId}
	</update>
	
	<!--비밀번호 찾기 -> 랜덤 비밀번호 -->
	<update id="changeMemberPw" parameterType="com.example.mbboard.dto.Member">
		update member set member_pw = #{memberPw}, pwcktime=now() where member_id = #{memberId} and email = #{email}
	</update>
	
	<!--email로 받은 임시 비밀번호 재설정-->
	<update id="reChangeMemberPw" parameterType="com.example.mbboard.dto.Member">
		UPDATE member set member_pw = #{newMemberPw}
			where member_pw = #{memberPw}
			and pwcktime is not null
			and timestampdiff(minute,pwcktime,now())<![CDATA[<]]>11
	</update>
	
</mapper>